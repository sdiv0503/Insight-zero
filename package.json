{
  "name": "insight-zero",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:engine": "source apps/engine/venv/bin/activate && uvicorn apps.engine.main:app --reload"
  }
}